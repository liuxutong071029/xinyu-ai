{
  "name": "xinyu-mindisle-server",
  "version": "5.0.0",
  "description": "心屿 MindIsle v5 后端 API（零外部依赖：仅 Node 内置 http/crypto 与 Node 22 全局 fetch；Supabase REST 或本地 JSON 双存储；OpenAI 兼容接口或内置规则 AI）",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "seed": "node seed.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}
